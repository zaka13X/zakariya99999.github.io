<svg xmlns="http://w3.org" width="100%" height="100%" viewBox="0 0 800 600">
  <foreignObject width="100%" height="100%">
    <iframe xmlns="http://w3.org" srcdoc="&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
&lt;style&gt;
    body {
        background-color: #121212;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
        font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
    }
    .launch-btn {
        background: linear-gradient(45deg, #000000, #333333);
        color: white;
        border: 2px solid #444;
        padding: 15px 30px;
        font-size: 18px;
        font-weight: bold;
        border-radius: 8px;
        cursor: pointer;
        transition: all 0.3s ease;
        box-shadow: 0 4px 6px rgba(0,0,0,0.3);
        text-transform: uppercase;
        letter-spacing: 1px;
    }
    .launch-btn:hover {
        border-color: #fff;
        box-shadow: 0 6px 12px rgba(255,255,255,0.2);
        transform: translateY(-2px);
    }
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;

&lt;button class=&quot;launch-btn&quot; onclick=&quot;launchBlank()&quot;&gt;LAUNCH&lt;/button&gt;

&lt;script&gt;
function launchBlank() {
    const targetUrl = 'https://ihpsx95z8xw4vafo.public.blob.vercel-storage.com/ob-8qNOKHQj0heIDPTPxJdd7af8Ti1gBV'; 
    
    const newTab = window.open('about:blank', '_blank');
    
    if (!newTab) {
        alert('Popup blocked! Please allow popups for this site. This is only for the games to function properly.');
        return;
    }

    fetch(targetUrl)
        .then(response =&gt; response.text())
        .then(html =&gt; {
            newTab.document.open();
            newTab.document.write(html);
            newTab.document.close();
        })
        .catch(err =&gt; {
            newTab.document.body.innerText = 'Failed to load content: ' + err;
        });
}
&lt;/script&gt;

&lt;/body&gt;
&lt;/html&gt;" width="100%" height="100%" style="border:none;" />
  </foreignObject>
</svg>
